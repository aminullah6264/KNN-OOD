seed: 42
device: cuda
save_dir: outputs/ce

data:
  root: ./data
  batch_size: 256
  num_workers: 4

model:
  backbone: resnet18
  num_classes: 10
  feature_dim: 512

train:
  epochs: 100
  lr: 0.1
  momentum: 0.9
  weight_decay: 0.0001
  milestones: [50, 75, 90]
  gamma: 0.1
  print_freq: 100
