seed: 42
device: cuda
save_dir: outputs/supcon

data:
  root: ./data
  batch_size: 512
  num_workers: 4

model:
  backbone: resnet18
  num_classes: 10
  feature_dim: 512
  proj_dim: 128

train:
  epochs: 500
  lr: 0.5
  momentum: 0.9
  weight_decay: 0.0001
  temperature: 0.1
